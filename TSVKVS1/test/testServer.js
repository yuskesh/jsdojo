var assert = require('assert');
var util = require('util');

var TsvKvsServer = require('../index').Server;

suite('Test Server', function(){ 
  suiteSetup(function(done) {
    done();
  });
  setup(function(done){
    done();
  });
  teardown(function(done){
    done();
  });
  suiteTeardown(function(done) {
    done();
  });
  test('test server...', function(done){
    done();
  });
});
